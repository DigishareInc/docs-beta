---
title: CRUD Operations
description: Create, read, update, and delete Third entities via the API.
icon: i-lucide-database
---


Manage your contacts with these endpoints. All requests require [authentication](/developer-guides/authentication).

---

## List Thirds

Retrieve all contacts with pagination and filtering.

::api-playground
---
method: GET
url: "https://api.digishare.ma/v1/thirds"
headers:
  Authorization: "Bearer YOUR_ACCESS_TOKEN"
responseSample:
  data:
    - id: "xYz123AbC"
      first_name: "Sarah"
      last_name: "Connor"
      email: "sarah@example.com"
      mobile: "+212612345678"
  meta:
    pagination:
      total: 150
      per_page: 15
      current_page: 1
      total_pages: 10
---
::

### Parameters

| Parameter  | Description                                |
| ---------- | ------------------------------------------ |
| `page`     | Page number (default: 1)                   |
| `limit`    | Items per page (default: 15, max: 100)     |
| `search`   | Search by name, email, or phone            |
| `orderBy`  | Sort by field (e.g., `created_at`)         |
| `sortedBy` | Sort direction: `asc` or `desc`            |
| `include`  | Load relations: `tags`, `groups`, `source` |

---

---

## Get a Third

Retrieve a single contact by ID.

::api-playground
---
method: GET
url: "https://api.digishare.ma/v1/thirds/{id}"
variables:
  id: "THIRD_ID"
headers:
  Authorization: "Bearer YOUR_ACCESS_TOKEN"
responseSample:
  data:
    id: "xYz123AbC"
    first_name: "Sarah"
    last_name: "Connor"
    name: "Sarah Connor"
    email: "sarah@example.com"
    mobile: "+212612345678"
    wa_id: "212612345678"
    address: "123 Main Street"
    lang: "en"
    premium: true
    verified: true
    data:
      company_name: "Cyberdyne"
      title: "Director"
    created_at: "2026-01-15T10:30:00Z"
---
::

### Parameters

| Parameter | Description                                                            |
| --------- | ---------------------------------------------------------------------- |
| `include` | Load relations: `tags`, `groups`, `source`, `tickets`, `conversations` |

---

---

## Create a Third

Add a new contact to your database.

::api-playground
---
method: POST
url: "https://api.digishare.ma/v1/thirds"
headers:
  Authorization: "Bearer YOUR_ACCESS_TOKEN"
  Content-Type: "application/json"
body:
  first_name: "John"
  last_name: "Doe"
  email: "john@example.com"
  mobile: "+212612345678"
  wa_id: "212612345678"
  lang: "en"
  tags: ["lead", "webinar"]
  data:
    company_name: "Acme Inc"
    title: "Manager"
responseSample:
  id: "xYz123AbC"
  status: "created"
---
::

---

### Available Fields

**Basic Info:** `first_name`, `last_name`, `name`, `email`, `mobile`, `phone`, `wa_id`

**Profile:** `address`, `address_2`, `lang`, `gender`, `birthday`

**Flags:** `is_company`, `premium`, `verified`, `status`

**Relations:** `source_id`, `country_id`, `state_id`, `city_id`, `parent_id`

**Arrays:** `tags`, `group_type_id`

**Extended:** `data` object (see [Overview](/developer-guides/thirds/overview#extended-data))

::note
**Automatic company assignment.** The `company_id` is automatically set from your authentication token. You don't need to pass it.
::

::tip
**Phone numbers are auto-formatted.** We standardize `mobile`, `phone`, and `wa_id` to international E.164 format.
::

### Response

Returns the created contact with `201 Created`.

---

## Update a Third

Modify an existing contact. Only include fields you want to change.

::api-playground
---
method: PATCH
url: "https://api.digishare.ma/v1/thirds/{id}"
variables:
  id: "THIRD_ID"
headers:
  Authorization: "Bearer YOUR_ACCESS_TOKEN"
  Content-Type: "application/json"
body:
  premium: true
  verified: true
  data:
    title: "Senior Director"
responseSample:
  id: "xYz123AbC"
  status: "updated"
---
::

---

::note
**Partial updates.** Fields not included in the request remain unchanged.
::

::warning
**Group replacement.** The `group_type_id` array replaces all existing groups. Include current groups to keep them.
::

### Response

Returns the updated contact with `200 OK`.

---

## Delete a Third

Remove a contact from your database.

::api-playground
---
method: DELETE
url: "https://api.digishare.ma/v1/thirds/{id}"
variables:
  id: "THIRD_ID"
headers:
  Authorization: "Bearer YOUR_ACCESS_TOKEN"
responseSample:
  status: "deleted"
---
::

::warning
**Soft delete.** The contact is marked as deleted but can be restored by an admin.
::

### Response

Returns `204 No Content` on success.

---

## Error Handling

### Common Errors

| Status | Meaning                  |
| ------ | ------------------------ |
| `401`  | Invalid or missing token |
| `403`  | Insufficient permissions |
| `404`  | Contact not found        |
| `422`  | Validation failed        |

### Validation Error Example

```json
{
  "message": "The given data was invalid.",
  "errors": {
    "wa_id": ["This WhatsApp ID is already in use."],
    "ext_ref": ["The external reference must be unique."]
  }
}
```

---

## What's Next?

::card-group
::card{title="Custom Fields" icon="i-lucide-tags" to="/developer-guides/thirds/custom-fields"}
Add custom metadata to your contacts.
::

::card{title="Back to Overview" icon="i-lucide-arrow-left" to="/developer-guides/thirds/overview"}
Review the Third object structure.
::
::
